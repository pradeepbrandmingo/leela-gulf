import HeroSection from "@/components/home/HeroSection";

export default function HomePage() {
  return (
    <main>
      {/* Hero Section — Full viewport video background with Industry typography grid */}
      <HeroSection />

      {/* Future home page sections will be assembled below */}
    </main>
  );
}
